import { auth } from './auth'
import { cat } from './cat'
import { classroom } from './classroom'
import { lookup } from './lookup'
import { user } from './user'

export const api = {
  cat,
  auth,
  user,
  classroom,
  lookup,
}
